Given a set of input values (x) and corresponding output values (y), 
train a linear regression model to find the relationship between them. 
Use the trained model to predict output values for new inputs.
